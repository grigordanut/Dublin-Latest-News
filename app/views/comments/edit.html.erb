<h1>Editing Comment</h1>

<%= render 'form', comment: @comment %>

<br/>

<%= link_to 'Back', article_comments_path(@article), class:"btn btn-dark" %>
<%= link_to 'Show', article_comment_path(@article, @comment), class:"btn btn-primary" %>
